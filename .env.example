# Database — required environment variable
# Copy this value into the root .env file.

# PostgreSQL connection string (Prisma format)
# REQUIRED
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/aastu_gibigubae?schema=public
